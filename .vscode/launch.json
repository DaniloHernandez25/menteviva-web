{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir index.html (Sin Localhost)",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
